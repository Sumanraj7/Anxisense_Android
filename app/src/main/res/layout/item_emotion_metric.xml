<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tvEmotionLabel"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Metric"
            android:fontFamily="@font/inter_family"
            android:textColor="@color/editorial_text_primary"
            android:textSize="14sp" />

        <TextView
            android:id="@+id/tvEmotionPercentage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0%"
            android:fontFamily="@font/inter_family"
            android:textColor="@color/editorial_text_primary"
            android:textSize="14sp"
            android:textStyle="bold" />

    </LinearLayout>

    <ProgressBar
        android:id="@+id/pbEmotion"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="2dp"
        android:layout_marginTop="8dp"
        android:progress="0"
        android:progressDrawable="@drawable/bg_editorial_card"
        android:progressTint="@color/editorial_text_primary"
        android:progressBackgroundTint="@color/editorial_divider"
        android:backgroundTint="@color/editorial_divider"/>

</LinearLayout>
